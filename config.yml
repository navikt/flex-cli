owner: navikt
repos:
    - name: flex-testdata-reset
      checks:
          - Bygg, test og push Docker image

    - name: flex-juridisk-vurdering-test-backend
      checks:
          - Bygg, test og push Docker image

    - name: sykepengesoknad-altinn
      checks:
          - Bygg, test og push Docker image

    - name: flex-fss-proxy
      checks:
          - Bygg, test og push Docker image

    - name: flex-juridisk-vurdering-test-frontend
      checks:
          - Bygg, test og push Docker image

    - name: sykepengesoknad-ikke-sendt-altinnvarsel
      checks:
          - Bygg, test og push Docker image

    - name: sykepengesoknad-korrigering-metrikk
      checks:
          - Bygg, test og push Docker image

    - name: sykepengesoknad-backend
      checks:
          - Bygg, test og push Docker image

    - name: flex-gateway
      checks:
          - Bygg, test og push Docker image

    - name: flex-vedtak-mock
      checks:
          - Bygg, test og push Docker image

    - name: flex-syketilfelle
      checks:
          - Bygg, test og push Docker image

    - name: spinnsyn-arkivering
      checks:
          - Bygg, test og push Docker image

    - name: sykepengesoknad-lovme-filter
      checks:
          - Bygg, test og push Docker image

    - name: sykepengesoknad-narmesteleder-varsler
      checks:
          - Bygg, test og push Docker image

    - name: spinnsyn-backend
      checks:
          - Checkout code and create docker tag

    - name: syfosoknadbrukernotifikasjon
      checks:
          - Checkout code and create docker tag

    - name: ditt-sykefravaer-backend
      checks:
          - bygg-test-deploy / Bygg, test og push Docker image

    - name: flex-inntektsmelding-status
      checks:
          - bygg-test-deploy / Bygg, test og push Docker image
